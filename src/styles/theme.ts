import themeColor from "./themeColor";
import themeFont from "./themeFont";
import themeSizes from "./themeSizes";
import { DefaultThemeStyles } from "./types";

const theme: DefaultThemeStyles = {
  color: themeColor,
  sizes: themeSizes,
  font: themeFont,
};

export default theme;
